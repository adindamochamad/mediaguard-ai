/** Model Claude untuk pipeline alert — gunakan model aktif (hindari varian deprecated). */
export const MODEL_CLAUDE_ANALIS = 'claude-sonnet-4-6';

/** Model Claude untuk chat interaktif Day 8. */
export const MODEL_CLAUDE_CHAT = 'claude-sonnet-4-6';

/** Batas karakter konten crawl yang dikirim ke Claude per permintaan. */
export const BATAS_KONTEN_CRAWL_KARAKTER = 10_000;
